# LSBEmbed
using in image steganography

embedLSB.hpp说明:
图像隐写时用到的LSB隐写方法。
支持嵌入的位数有1, 2, 3.不提供更大的嵌入位数是因为这会导致图像质量的下降。

注意：如果需要隐写的数据比较小，而图像比较大。这里的嵌入方案中不会平分这些
要嵌入的比特。这导致的结果可能有：lSB等于2的时候，前面的像素嵌入了2个比特
，而后面的像素则没有嵌入任何东西。


